<!doctype html>
<html lang="az">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ClassRoom</title>

    <link rel="stylesheet" href="~/css/base.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
   
    
</head>
<body>

    <header class="site-header" style="height:130px">
        <div class="container header-shell">
            <div class="header-inner">
                <a class="btn primary" asp-controller="Class" asp-action="Dashboard">Dashboard</a>
                <a class="brand" href="~/">
                    <div class="logo"></div>
                    <div>
                        <div class="title">ClassRoom</div>
                        <div class="subtitle">Class • Quiz • Task • Attendance</div>
                    </div>                    
                </a>

                <div class="header-actions">
                    <button class="icon-btn mobile-toggle" data-mobile-toggle title="Menu">
                        <span class="icon" style="--mask:url('data:image/svg+xml;utf8,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22 fill=%22black%22><path d=%22M4 6h16v2H4V6zm0 5h16v2H4v-2zm0 5h16v2H4v-2z%22/></svg>');"></span>
                    </button>
                    @if (Context.Request.Cookies.ContainsKey("AccessToken"))
                    {
                        <a class="btn" asp-controller="Class" asp-action="Logout">Logout</a>
                    }
                    else
                    {
                        <a class="btn" asp-controller="Home" asp-action="Login">Login</a>
                    }
                </div>
            </div>

            <div class="mobile-panel" data-mobile-panel>
                <nav class="menu">
                    <a class="btn primary" asp-controller="Home" asp-action="ClassRoom">Class</a>
                    @if (Context.Request.Cookies.ContainsKey("AccessToken"))
                    {
                        <a class="btn" asp-controller="Class" asp-action="Logout">Logout</a>
                    }
                    else
                    {
                        <a class="btn" asp-controller="Home" asp-action="Login">Login</a>
                    }
                </nav>
            </div>
        </div>
    </header>

    <!-- main container qalsın -->
    <main class="container main-shell">
        @RenderBody()
    </main>

    <footer class="site-footer" style="height:108px">
        <div class="container footer-shell">
            <div class="footer-inner">
                <div class="smallprint">© <span id="y"></span> ClassRoom • MVC + API</div>
            </div>
        </div>
    </footer>

    <div class="toast" id="toast"><div class="t"></div><div class="d"></div></div>

    <script src="~/js/base.js"></script>
    <script>
        const y = document.getElementById("y");
        if (y) y.textContent = new Date().getFullYear();
    </script>
</body>
</html>
