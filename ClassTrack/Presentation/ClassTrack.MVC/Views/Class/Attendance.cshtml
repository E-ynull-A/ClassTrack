


<link rel="stylesheet" href="~/css/attendance.css">
<div class="topbar">
    <div>
        <div class="h1">Attendance</div>
        <div class="muted">
            Date: <span id="dateText">2026-01-24</span> • Role:
            <span class="badge teacher" id="roleBadge">Teacher</span>
        </div>
    </div>
    <div class="nav">
        <button class="btn" id="btnAllPresent">All Present</button>
        <button class="btn" id="btnAllAbsent">All Absent</button>
        <button class="btn ok" id="btnSave">Save (MVC POST)</button>
    </div>
</div>

<div class="card">
    <div class="h2">Students</div>
    <div class="muted">Status: Absent(0) • Present(1) • Late(2)</div>
    <div class="line"></div>

    <div class="att-list" id="list">
        <!-- Row sample (server-side render edəcəksən) -->
        <div class="att-row" data-student="1">
            <div class="name">
                <div class="n">Ali M.</div>
                <div class="muted">ID: 1</div>
            </div>

            <div class="seg" role="group" aria-label="Attendance status">
                <label class="seg-opt absent">
                    <input type="radio" name="att-1" value="0" checked>
                    Absent
                </label>
                <label class="seg-opt present">
                    <input type="radio" name="att-1" value="1">
                    Present
                </label>
                <label class="seg-opt late">
                    <input type="radio" name="att-1" value="2">
                    Late
                </label>
            </div>

            <input class="input note" placeholder="note (optional)" />
        </div>

        <div class="att-row" data-student="2">
            <div class="name">
                <div class="n">Aysel K.</div>
                <div class="muted">ID: 2</div>
            </div>

            <div class="seg" role="group" aria-label="Attendance status">
                <label class="seg-opt absent">
                    <input type="radio" name="att-2" value="0">
                    Absent
                </label>
                <label class="seg-opt present">
                    <input type="radio" name="att-2" value="1" checked>
                    Present
                </label>
                <label class="seg-opt late">
                    <input type="radio" name="att-2" value="2">
                    Late
                </label>
            </div>

            <input class="input note" placeholder="note (optional)" />
        </div>
    </div>

    <div class="muted" style="margin-top:10px">
        MVC-də submit edəndə hər student üçün (StudentId, Status enum int, Note) göndərəcəksən.
    </div>
</div>
<div class="toast" id="toast"><div class="t"></div><div class="d"></div></div>

<script src="~/js/attendance.js"></script>
